#!/bin/env zsh

CAT_PROG=(less)
LS_PROG=(exa -lgHh --git --group-directories-first)

function na {
    local OBJ=$1
    if [[ -f $OBJ ]]; then $CAT_PROG[@] $OBJ; fi;
    if [[ -d $OBJ ]]; then $LS_PROG[@] $OBJ; fi;
}

na $@
